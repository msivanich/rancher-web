---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: rancher-web
  name: rancher-web-deployment
  labels:
    app: rancher-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rancher-web
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: rancher-web
    spec:
      affinity:
        podAffinity: {}
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - podAffinityTerm:
              labelSelector: {}
              topologyKey: kubernetes.io/hostname
            weight: 1
      containers:
      - image: registry.rmntest.com/private-httpd
        imagePullPolicy: Always
        name: rancher-web
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /usr/local/apache2/htdocs
          name: webroot
          subPath: rancherweb/htdocs
        - mountPath: /usr/local/apache2/conf
          name: webroot
          subPath: rancherweb/conf    
      volumes:
      - name: webroot
        persistentVolumeClaim:
          claimName: rancher-web-pvclaim